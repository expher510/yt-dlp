name: Download YouTube Video

on:
  workflow_dispatch:
    inputs:
      url:
        description: 'YouTube URL'
        required: true
      folder_id:
        description: 'Google Drive Folder ID'
        required: true

jobs:
  download:
    runs-on: ubuntu-latest
    steps:
      - name: Setup yt-dlp
        uses: AnimMouse/setup-yt-dlp@v3

      - name: Setup YouTube cookies
        uses: AnimMouse/setup-yt-dlp/cookies@v3
        with:
          cookies: ${{ secrets.YOUTUBE_COOKIES }}

      - name: Download Video
        run: yt-dlp -o "video.%(ext)s" "${{ github.event.inputs.url }}"

      - name: Send to n8n
        run: |
          FILE=$(ls video.*)
          curl -X POST "${{ secrets.N8N_WEBHOOK_URL }}" \
            -F "video=@$FILE" \
            -F "folder_id=${{ github.event.inputs.folder_id }}" \
            -F "url=${{ github.event.inputs.url }}"

      - name: Update YouTube cookies
        uses: AnimMouse/setup-yt-dlp/cookies/update@v3
        with:
          cookies_secret_name: YOUTUBE_COOKIES
          token: ${{ secrets.GH_PAT }}
