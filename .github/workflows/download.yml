name: Download YouTube Video
on:
  workflow_dispatch:
    inputs:
      url:
        description: 'YouTube URL'
        required: true

jobs:
  download:
    runs-on: ubuntu-latest
    steps:

      - name: Setup yt-dlp
        uses: AnimMouse/setup-yt-dlp@v3

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install bgutil POT Provider
        run: |
          pip install bgutil-ytdlp-pot-provider
          git clone --single-branch --branch 1.3.1 \
            https://github.com/Brainicism/bgutil-ytdlp-pot-provider.git \
            ~/bgutil-ytdlp-pot-provider
          cd ~/bgutil-ytdlp-pot-provider/server/
          npm ci
          npx tsc
          ls build/  # تأكد إن الـ build اتعمل

      - name: Start POT Server
        run: |
          cd ~/bgutil-ytdlp-pot-provider/server/
          node build/main.js &
          sleep 5
          curl -s http://127.0.0.1:4416/ping  # تأكد إن السيرفر شغال

      - name: Download Video
        run: |
          yt-dlp -v \
            -o "video.%(ext)s" \
            "${{ github.event.inputs.url }}"

      - name: Send to n8n
        if: success()
        run: |
          FILE=$(ls video.* | head -1)
          echo "File found: $FILE"
          curl -X POST "${{ secrets.N8N_WEBHOOK_URL }}" \
            -F "video=@${FILE}" \
            -F "url=${{ github.event.inputs.url }}" \
            -F "status=success"

      - name: Send error to n8n
        if: failure()
        run: |
          curl -X POST "${{ secrets.N8N_WEBHOOK_URL }}" \
            -H "Content-Type: application/json" \
            -d "{\"status\": \"error\", \"url\": \"${{ github.event.inputs.url }}\"}"
